# ========================================
# MCP PROMETHEUS - RUNTIME ENV VARS
# ========================================

# Prometheus Connection (REQUIRED)
PROMETHEUS_URL=http://prometheus-server:9090

# Logging
LOG_LEVEL=INFO

# Optional: Polling mode
POLLING_ENABLED=false
POLLING_INTERVAL=300

# Optional: Custom metrics whitelist (comma-separated)
# ALLOWED_METRICS=node_cpu_seconds_total,node_memory_MemAvailable_bytes